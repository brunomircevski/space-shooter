package Entity.Bullet;

import java.awt.image.BufferedImage;

public class BulletBasic extends Bullet implements IBullet {

    public BulletBasic(int y, BufferedImage image, int speed) {
        super(y, image, speed);
    }

}
